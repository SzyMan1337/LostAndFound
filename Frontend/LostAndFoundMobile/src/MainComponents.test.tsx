//import { render } from '@testing-library/react-native';
import { Text } from 'react-native';
import {
  MainContainer,
  MainTitle,
  Subtitle,
  InputSection,
  CustomTextInput,
  MainButton,
  SecondaryButton,
  PressableText,
  ScoreView,
} from './Components';

test('When InputSection component is rendered, it should contain correct title', () => {
  /*const { queryByText } = render(
    <InputSection title="Title test"></InputSection>,
  );

  const title = queryByText('Title test');
  expect(title).not.toBeNull();*/
});
