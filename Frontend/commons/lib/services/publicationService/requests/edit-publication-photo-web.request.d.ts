import { PublicationResponseType } from "../publicationTypes";
export declare const editPublicationPhotoWeb: (publicationId: string, photo: File, accessToken: string) => Promise<PublicationResponseType | undefined>;
